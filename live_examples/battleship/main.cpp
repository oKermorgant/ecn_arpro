#include <player.h>
#include <cstdlib>
#include <ctime>
#include <random>

int main()
{
  srand(time(nullptr));
  Player p1, p2;

  p1.display();


  /*

  while(true)
  {
    p1.display();
    p1.target(p2);
    if(not p2.stillAlive())
      break;

    p2.display();
    p2.target(p1);
    if(not p1.stillAlive())
      break;

  }

  */













}
